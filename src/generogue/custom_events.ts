
export enum EventType {
    /** change game status. Associated data : GameStatus. Triggered when player activate a down stair */
    CHANGE_STATUS,
    /** starts a new game from in-game menu. No associated data */
    NEW_GAME,
}
